.container
  %h2 Moje anulowane realizacje
  .panel.panel-warning
    .panel-heading
      .panel-title
        Tutaj znajdują się Twoje anulowane realizacje oczekujące na przejęcie. Nie ignoruj ich, podejmij kroki, by zostały przejęte!
  - if cancelled_user_productions.any?
    %table.table.table-hover
      %thead
        %tr
          %td Od
          %td Do
          %td Actions
      %tbody
        - cancelled_user_productions.each do |prod|
          %tr
            %td= prod.start_date
            %td= prod.end_date
            %td
              = link_to claim_production_path(prod), data: { confirm: 'Are you sure?' }, class: 'btn btn-primary' do
                %span.glyphicon.glyphicon-refresh
                  %strong Anuluj anulowanie... (zdecyduj się?)
  - else
    .panel.panel-success
      .panel-heading
        .panel-title
          Nie anulowałeś żadnych realizacji :) jeszcze... ;)
