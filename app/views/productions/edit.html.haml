.container
  = simple_form_for production, html: { class: 'form-horizontal' } do |f|
    .row
      .col-lg-12
        = f.label :user
        = f.collection_select :user, User.all, :id, :email
    .row
      .col-lg-12
        = f.submit 'Save', class: 'btn btn-default'
